'''
    Python使用register方式
        抽象基类作为装饰器使用。ABC.register，在Python3.3之前的版本不能这样使用，只能在定义类之后像普通函数那样调用
        更常见的还是作为函数使用，用于注册其他地方定义的类。
'''


if __name__ == '__main__':
    # Tombola.register(BingoCage)
    # Tombola.register(LottoBlower)
    # Tombola.register(Tombolist)
    pass
